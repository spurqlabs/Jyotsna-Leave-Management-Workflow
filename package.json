{
  "name": "orangehrm-automation",
  "version": "1.0.0",
  "description": "BDD Test Automation Framework for OrangeHRM using Playwright, Cucumber, and Node.js",
  "main": "index.js",
  "scripts": {
    "test": "cucumber-js features/**/*.feature --require features/step_definitions/**/*.js --require features/support/**/*.js",
    "test:headed": "HEADED=true cucumber-js --require features/step_definitions/**/*.js --require features/support/**/*.js",
    "test:smoke": "cucumber-js --require features/step_definitions/**/*.js --require features/support/**/*.js --tags '@smoke'",
    "test:regression": "cucumber-js --require features/step_definitions/**/*.js --require features/support/**/*.js --tags '@regression'",
    "test:negative": "cucumber-js --require features/step_definitions/**/*.js --require features/support/**/*.js --tags '@negative'",
    "test:timesheet": "cucumber-js --require features/step_definitions/**/*.js --require features/support/**/*.js --tags '@timesheet'",
    "test:parallel": "cucumber-js --require features/step_definitions/**/*.js --require features/support/**/*.js --parallel 2",
    "report": "node src/utils/reportGenerator.js",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write 'src/**/*.js'"
  },
  "keywords": [
    "playwright",
    "cucumber",
    "bdd",
    "automation",
    "testing"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^8.11.1",
    "@playwright/test": "^1.40.1",
    "@reportportal/agent-js-cucumber": "^5.6.2",
    "allure-commandline": "^2.25.0",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1"
  },
  "dependencies": {
    "@cucumber/messages": "^32.0.1",
    "allure-cucumberjs": "^3.4.5",
    "cucumber-html-reporter": "^6.0.0",
    "fs-extra": "^11.2.0",
    "winston": "^3.11.0"
  }
}
